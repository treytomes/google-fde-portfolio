# ── Development path — free Gemini API (Days 1–3) ─────────────────────────────
# google-generativeai is deprecated; use google-genai
google-genai

# ── Notebook + data ───────────────────────────────────────────────────────────
jupyter
numpy

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv

# ── Evaluation (Day 3) ────────────────────────────────────────────────────────
ragas
datasets
langchain-google-genai
ollama           # Ollama Python client — local LLM fallback
langchain-ollama # LangChain wrapper for Ollama — used by RAGAS judge fallback

# ── Vertex AI verification path (Day 4) ───────────────────────────────────────
# google-genai already supports Vertex AI via genai.Client(vertexai=True, ...)
# google-cloud-aiplatform is NOT needed — we use google-genai throughout

# ── Orchestration stretch (Day 5 — uncomment when ready) ──────────────────────
# langgraph
# langchain-google-vertexai
